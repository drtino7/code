const varia = "tablet samsung";

let replace = varia.replace("tablet", "laptop");
let slice = varia.slice(0,5);
slice = varia.slice(5);
// substring change if substring(2,1)
let substring = varia.substring(0,10);
let first = varia.charAt(0);