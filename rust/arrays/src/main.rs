fn main() {
   //arrays
   //its fixed size, stored in stack. signature: T, lenght.
   let array: [i32;5] = [1,2,3,4,5];
   let len:i32 = array.len();
   let list: [132:100] = [1;100]; //repeats 1, 100 times 
   //let arr = [1,2,'3']; //error 

}
