fn main() {
        //if

    //let number = 7;
   // if number{ // need bool
    //    printnl!("{}",number)
    //}

    // > greter than
    // < less than
    // <= less than or equal
    // >= greter than or equal
    // != not equal

    // && and
    // || or
    // ! not


    let var = "juan";
    if var == "roberto"{
        println!("roberto");
    } else if var == "rodrigo"{
        println!("rodrigo");
    } else{
        println!("is not rodrigo nither juan");
    }

    // numb % 4 == 0 // rest

    let condition = true;
    let number = if condition {1} else {0};
}
